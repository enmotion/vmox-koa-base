export const env = {
  // ... existing env variables ...
  
  // 文件上传配置
  UPLOAD_DIR: process.env.UPLOAD_DIR || 'public/uploads',  // 文件上传目录，默认为 public/uploads
  
  // ... existing env variables ...
} 