{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "启动Koa程序",
      "program": "${workspaceFolder}/server/app.js",
      "cwd": "${workspaceFolder}"
    }
  ]
}